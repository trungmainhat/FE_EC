export const isEditProfileSelector = (state) => state.profile.isEdit;
export const profileSelector = (state) => state.profile.profile;
// export const customerByIdSelector = (state) => state.customer.customer;
