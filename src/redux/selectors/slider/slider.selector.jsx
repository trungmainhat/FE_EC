export const isAddSelectorSlider = (state) => state.slider.isAdd;
export const isEditSelectorSlider = (state) => state.slider.isEdit;
export const isSliderSelector = (state) => state.slider.slider;
export const isResetSelectorSlider = (state) => state.slider.isReset;
export const isStatusSelectorSlider = (state) => state.slider.status;
export const isSortSelectorSlider = (state) => state.slider.sort;
