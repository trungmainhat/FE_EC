export const isAddSelectorReview = (state) => state.review.isAdd;
export const isEditSelectorReview = (state) => state.review.isEdit;
export const isSelectorReview = (state) => state.review.review;
export const isResetSelectorReview = (state) => state.review.isReset;
export const isStatusSelectorReview = (state) => state.review.status;
export const isSortSelectorReview = (state) => state.review.sort;
